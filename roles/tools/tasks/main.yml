---
- name: Install Formulas
  community.general.homebrew:
    name: "{{ tools_formulas }}"
    state: latest

- name: Install Casks
  community.general.homebrew_cask:
    name: "{{ tools_casks }}"
    state: latest
